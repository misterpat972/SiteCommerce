{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block containe %}   

{% if notification %}
    <div class="alert alert-info" id="notification">
        {{ notification }}
    </div>
{% endif %}

 
<h1>Modifier mon mot de passe</h1>
<a href="{{path('compte')}}">Retourner à mon compte</a><br>
<hr>
<h2>Formulaire</h2>
{{form_start(form)}}
{{form_widget(form)}}
{# <button type="submit" class="btn btn-primary">Modifier mon mot de passe</button> #}
{{form_end(form)}}
{% endblock %}

